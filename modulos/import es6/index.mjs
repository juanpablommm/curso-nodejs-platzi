/**para realizar la importacion de un modulo con las sintaxis de ecmaScript 6
 * ya no utlizams la creacion de una constante en la cual le definimos el valor
 * con la importacion al utlizar la plabra require
 * 
 * ahora utlizamos la palabra import, el nombre que le vamos a dar al modulo que vamos a 
 * importary la plabra clave from pra saber de donde los vamos a importar y entre 
 * commilas poenmos la ruta
 */
import modulo from './modulo.mjs'

modulo();